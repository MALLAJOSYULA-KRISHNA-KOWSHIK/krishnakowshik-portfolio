const Footer = () => (
  <footer className="border-t border-primary/10 py-6 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto flex flex-wrap items-center justify-between gap-4">
    <span className="font-mono text-xs text-muted-foreground">// kowshik_ © 2026</span>
    <a
      href="#"
      className="font-mono text-xs text-primary hover:text-glow-cyan transition-colors"
    >
      [↑ RETURN TO TOP]
    </a>
  </footer>
);

export default Footer;
